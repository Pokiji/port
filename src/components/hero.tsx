type HeroProps = {
  eyebrow?: string;
  title?: string;
  subtitle?: string;
}


export default function Hero({

}: HeroProps) {
  return (
    <section className="min-h-screen flex items-center px-6">

    </section>
  );
}